{
  "name": "frontend-image-compress",
  "version": "3.0.0",
  "description": "compress image-type file in frontend using canvas",
  "main": "./dist/frontend-compress.js",
  "typings": "./index.d.ts",
  "scripts": {
    "watch": "parcel watch ./src/frontend-compress.ts --global Compress --no-source-maps",
    "build": "parcel build ./src/frontend-compress.ts --global Compress --out-file frontend-compress.min.js --no-source-maps"
  },
  "keywords": [
    "image-compress",
    "canvas",
    "file-compress",
    "frontend-compress"
  ],
  "author": "xhmm",
  "license": "MIT",
  "repository": "https://github.com/XHMM/frontend-image-compress",
  "devDependencies": {
    "parcel-bundler": "^1.12.2",
    "typescript": "3.2.1"
  }
}
